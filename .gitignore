# Environment variables / secrets
.env
.env.local
.env.*.local
.env.test
# (.env.example and .env.test.example are intentionally tracked)

# Dependencies
node_modules/

# Next.js
/.next/
/out/
/build
*.tsbuildinfo
next-env.d.ts
.next-profiles/

# Testing
/coverage
/test-results/
/playwright-report/
/playwright/.cache/
.playwright-mcp/

# Vercel
.vercel

# Supabase CLI local-stack state (generated by `supabase start`/`db reset`;
# includes local-only demo keys for the Docker stack, never production)
supabase/.branches
supabase/.temp

# Misc
.DS_Store
*.pem

# Local Claude Code settings (per-developer; keep settings.json tracked)
.claude/settings.local.json
